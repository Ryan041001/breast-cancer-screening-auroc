experiment:
  name: retune_b8_e12

paths:
  project_root: ..
  train_csv: train.csv
  train_images: train_img
  test_images: test_img
  submission_template: name_sid_submission.csv
  output_root: outputs

runtime:
  seed: 42
  device: cuda

train:
  folds: 5
  batch_size: 8
  image_size: 384
  epochs: 12
  num_workers: 0
