experiment:
  name: smoke_fusion_mlp_gelu_d0

paths:
  project_root: ..
  train_csv: train.csv
  train_images: train_img
  test_images: test_img
  submission_template: name_sid_submission.csv
  output_root: outputs

runtime:
  seed: 7
  device: cuda

train:
  folds: 5
  batch_size: 2
  image_size: 384
  epochs: 1
  num_workers: 0
  fusion_head_variant: mlp
  fusion_hidden_dim: 512
  fusion_dropout: 0.0
  fusion_activation: gelu
