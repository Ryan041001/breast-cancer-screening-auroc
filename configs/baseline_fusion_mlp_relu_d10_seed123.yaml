experiment:
  name: baseline_fusion_mlp_relu_d10_seed123

paths:
  project_root: ..
  train_csv: train.csv
  train_images: train_img
  test_images: test_img
  submission_template: name_sid_submission.csv
  output_root: outputs

runtime:
  seed: 123
  device: cuda

train:
  folds: 5
  batch_size: 16
  image_size: 384
  epochs: 10
  num_workers: 0
  transform_profile: baseline
  fusion_head_variant: mlp
  fusion_hidden_dim: 512
  fusion_dropout: 0.1
  fusion_activation: relu
