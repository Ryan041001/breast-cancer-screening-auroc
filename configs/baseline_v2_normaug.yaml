experiment:
  name: baseline_v2_normaug

paths:
  project_root: ..
  train_csv: train.csv
  train_images: train_img
  test_images: test_img
  submission_template: name_sid_submission.csv
  output_root: outputs

runtime:
  seed: 42
  device: cuda

train:
  folds: 5
  batch_size: 16
  image_size: 384
  epochs: 10
  num_workers: 0
  transform_profile: normaug
