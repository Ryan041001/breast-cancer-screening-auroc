experiment:
  name: baseline_v2_normaug_splitv2

paths:
  project_root: ..
  train_csv: train.csv
  train_images: train_img
  test_images: test_img
  submission_template: name_sid_submission.csv
  output_root: outputs

runtime:
  seed: 42
  device: cuda
  fold_seed: 42
  train_seed: 42
  warmup_seed: 42

train:
  folds: 5
  batch_size: 16
  image_size: 384
  epochs: 10
  num_workers: 4
  transform_profile: normaug
  fusion_eval_reference_run: baseline_mammonet32k_warmup_e4_lr5e4_f5_e6_freeze1_cosine_pairedlr5e4_splitv2
